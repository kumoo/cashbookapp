{
	"version": 2,
	"installCommand": "pnpm install && pnpx prisma generate && pnpx prisma migrate deploy",
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/index.ts"
		}
	]
}

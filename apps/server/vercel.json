{
	"version": 2,
	"buildCommand": "prisma generate && prisma migrate deploy && tsup src/index.ts --format cjs",
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/index.ts"
		}
	]
}
